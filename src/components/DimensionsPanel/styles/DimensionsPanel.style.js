import { colors } from '../../../modules/colors'

export const styles = {
    divContainer: {
        height: '100%',
        display: 'flex',
        flexDirection: 'column',
        backgroundColor: colors.snow,
        padding: '8px',
    },
    textField: {
        paddingBottom: '6px',
    },
}
