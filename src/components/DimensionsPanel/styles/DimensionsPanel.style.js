import { colors } from '../../../modules/colors'

export const styles = {
    divContainer: {
        height: '100%',
        display: 'flex',
        flexDirection: 'column',
        backgroundColor: colors.snow,
        padding: '8px',
        overflow: 'hidden',
    },
    textField: {
        paddingBottom: '6px',
    },
}
