import { colors } from '../../../modules/colors'

export const styles = {
    divContainer: {
        height: '100%',
        display: 'flex',
        flexDirection: 'column',
        backgroundColor: colors.snow,
    },
    textField: {
        paddingBottom: '12px',
        width: 'calc(100% - 8px)',
    },
}
