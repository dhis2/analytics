import { theme } from '@dhis2/ui'

export const styles = {
    recommendedIcon: {
        backgroundColor: theme.secondary300,
        height: '8px',
        width: '8px',
        borderRadius: '4px',
        marginLeft: '5px',
        display: 'inline-block',
        cursor: 'pointer',
    },
}
